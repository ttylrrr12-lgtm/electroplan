name: PUE_RU_v1
channels:
  vertical_only: true
  horizontal_only: true
  default_channel_height_from_floor_mm: 200
  default_offset_from_wall_mm: 30
forbidden_zones:
  - type: around_opening
    margin_mm: 100
costs:
  per_meter: 1.0     # базовая стоимость метра (для эвристики A*)
  wall_crossing: 50.0
  channel_break: 10.0
devices:
  socket:
    recommended_height_mm: 300
  switch:
    recommended_height_mm: 900
